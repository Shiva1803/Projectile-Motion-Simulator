version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
      - ./fortran:/app/fortran
    environment:
      - FLASK_ENV=development
    command: python backend/app.py
